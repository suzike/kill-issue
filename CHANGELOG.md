# Changelog

本文件记录 kill-issue skill 的版本变更。版本规则：语义化版本（大版本.功能版本.修复版本）。

## v1.1.0（2025-08-13）

### 新增

- **目录结构规整（第 0 步重构）**：统一通用上下文为 `000-context/`（命名不规范直接改名），问题文件夹内部按 `001/`（问题描述与专属上下文）`002/`（中间过程数据）`003/`（最终报告与证据）三级分级；自动建立 `问题描述.md` 并整理文件清单
- **苏格拉底式逐个澄清提问（第 2 步重构）**：全量扫描 → 提炼 → 过滤疑问项，**逐个问、一次只问一个**，每题附 4 个选项（A/B/C/D）+ 推荐答案 + 允许自定义输入
- **通用问题排查报告模板**（`report-general.md`）：面向正式完整交付（对外汇报、项目总结、跨部门流转），含调查时间线、影响面与严重度、解决方案与验证、术语与附录
- **上下文材料分级与冲突仲裁**：必须（模型/生成代码）、条件必需（DBC/接口表，问题附件含报文时必给）、按需（需求/标定/测试说明/共享日志）；材料冲突时以模型和代码为准（模型 vs 生成代码不一致以生成代码为准）
- **CANoe/CANape 日志解析指引**（`log-reading.md` 扩展）：.asc/.blf/.mf4/.dat/.rec 报文与测量数据解析套路，DBC 缺失先索要不硬猜

### 修复

- `structure.svg` / `workflow.svg` 配图：压线、压字、箭头悬空与未对齐卡片边缘等问题
- 报告模板内部引用同步为三级目录结构（`001/问题描述.md`、`001/logs/`、`003/证据/`）

### 文档

- README、`docs/kill-issue-使用说明.md`、`dist/kill-issue-User-Guide.md` 同步新目录结构与澄清流程

## v1.0.0（2025-08-11）

首个正式版。

- 通用排查方法论：7 步流程（初始化并规整上下文 → 收集输入并自动生成问题描述 → 澄清提问 → 判定归属层 → 定位证据 → 选模板出报告 → 登记台账）
- 5 种报告模板：根因分析（默认）/ 通用问题排查 / 5W / 8D / 轻量简报
- 初始化脚本 `init_context.py`（建上下文目录、台账、问题文件夹、`--organize` 启发式归类）
- 报告写作硬性规范：结论先行、图文并茂、**禁信号名堆砌（头号禁令）**、信号名必配语义翻译、归因方向准确
- 完整文档：README + 使用说明 + 用户手册
